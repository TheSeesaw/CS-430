// CS430_Project1.cpp : Defines the entry point for the console application.
//
// By: Kristoffer Schindele
#include <stdlib.h>
#include <stdio.h>

// customized reading/writing routines

//assume file is already open
//convert to P3
void convertP3() {
	//read through file
	// container variable for current ASCII value
	int ac;
	while (fscanf(fh, "%c", c1) != EOF) {
		fprint(fgetc(fh));

	}
	fprint("End of file.");
}

int main(int argc, char *argv[]) {

	// argument checking
	if (argc != 3) {
		fprintf(stderr, "Invalid number of arguments.");
		return(1);
	}
	else if (argv[2] == NULL || argv[3] == NULL) {
		fprintf(stderr, "Error reading or writing file.");
	}

	FILE* fh = fopen(argv[2], "r"); // open file for reading
	// check file header for type
	// read in file header
	// store first character in dummy variable
	int p = fgetc(fh);
	// store MAGIC NUMBER in t
	int t = fgetc(fh);
	//jump to appropriate routine
	switch (t) {
	case 3:
		convertP3();
		break;
	//case 6:
		//convertP6();
		//break;
	//case 7:
		//convertP7();
		//break;
	default:
		fprintf(stderr, "Invalid file.");
	}
	
}
